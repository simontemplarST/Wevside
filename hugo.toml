baseURL = "https://cameronheard.com"
languageCode = "en-us"
title = "N0YEP — Amateur Radio Station"
enableRobotsTXT = true

# RSS only for /blog/ — disable the default feeds everywhere else.
# The blog section re-enables RSS via its _index.md front matter.
[outputs]
  home = ["HTML"]
  section = ["HTML"]
  taxonomy = ["HTML"]
  term = ["HTML"]

[params]
  callsign = "N0YEP"
  operator = "Cameron Heard"
  qth = "Grid EN33"
  licenseClass = "General"
  tagline = "CQ CQ CQ de N0YEP"
  description = "Personal amateur radio station log, projects, and notes."
  # On-air light: the callsign PSKReporter watches. A scheduled GitHub Action
  # refreshes static/on-air.json from PSKReporter (see .github/workflows/hugo.yml
  # and scripts/on-air-light.py); the header badge reads that file.
  onAirCallsign = "N0YEP"
  # Minutes since the last spot to still count as "on air".
  onAirFreshMin = 12

[menu]
  [[menu.main]]
    name = "Home"
    pageRef = "/"
    weight = 10
  [[menu.main]]
    name = "Logbook"
    pageRef = "/logbook"
    weight = 20
  [[menu.main]]
    name = "Map"
    pageRef = "/map"
    weight = 25
  [[menu.main]]
    name = "Blog"
    pageRef = "/blog"
    weight = 30
  [[menu.main]]
    name = "Station"
    pageRef = "/station"
    weight = 40
  [[menu.main]]
    name = "Contact"
    pageRef = "/contact"
    weight = 50

[minify]
  # The tdewolff JS minifier silently drops our inline map script; keep JS as-is.
  disableJS = true

[markup]
  [markup.goldmark.renderer]
    unsafe = true
  [markup.highlight]
    noClasses = true
    style = "bw"

[taxonomies]
  hobby = "hobbies"

[params.social]
  qrz = "https://www.qrz.com/db/N0YEP"
  github = "https://github.com/"
